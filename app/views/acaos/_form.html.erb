<!-- app/views/acaos/_form.html.erb -->
<%= form_with(model: acao, local: true) do |form| %>
  <% if acao.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(acao.errors.count, "error") %> prohibited this record from being saved:</h2>
      <ul>
        <% acao.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :descricao %>
    <%= form.text_area :descricao %>
  </div>

  <div>
    <%= form.label :pontos_obtidos %>
    <%= form.number_field :pontos_obtidos %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
