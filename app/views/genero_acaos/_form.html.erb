<!-- app/views/genero_acaos/_form.html.erb -->
<%= form_with(model: genero_acao, local: true) do |form| %>
  <% if genero_acao.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(genero_acao.errors.count, "error") %> prohibited this record from being saved:</h2>
      <ul>
        <% genero_acao.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :descricao %>
    <%= form.text_field :descricao %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
