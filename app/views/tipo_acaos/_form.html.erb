<!-- app/views/tipo_acaos/_form.html.erb -->
<%= form_with(model: tipo_acao, local: true) do |form| %>
  <% if tipo_acao.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tipo_acao.errors.count, "error") %> prohibited this record from being saved:</h2>
      <ul>
        <% tipo_acao.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :descricao %>
    <%= form.text_field :descricao %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
